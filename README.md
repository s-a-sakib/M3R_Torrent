# M3R Torrent

Public dashboard for the M3R network.

## Run

1. Start the Spring Boot node on `http://localhost:3000`
2. Start the dashboard:

```bash
cd Torrent
npm start
```

Then open `http://localhost:4173`.

The backend URL is editable in the top bar and defaults to:

`http://localhost:3000/torrent/api`
